Para crear un modelo:
php artisan make:model Models/(tabla)
Crea la tabla y la migracion en el dir Models

Para crear un controlador:
php artisan make:controller (nombre del controller)
Agregar en routes.php para que use el controlador
Route::resource('domain', '(nombre del controller)');

